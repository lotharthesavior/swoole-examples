diff --git a/example1/server2.php b/example2/server2.php
index 66f631d..f3ef7ac 100644
--- a/example1/server2.php
+++ b/example2/server2.php
@@ -18,22 +18,25 @@ $server->on("start", function($server) {
     echo 'Server started with PID: ' . $server->master_pid . " at http://127.0.0.1:8181\n";
 });
 
-function get_users(): array {
+function curl_request(string $url) {
 	$ch = curl_init();
-    curl_setopt($ch, CURLOPT_URL, "localhost:8080");
+    curl_setopt($ch, CURLOPT_URL, $url);
     curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
     $data = curl_exec($ch);
     curl_close($ch);
 	return json_decode($data, true);
 }
 
+function get_users(): array {
+	return curl_request("localhost:8080");
+}
+
 function get_users_posts(string $email): array {
-	$ch = curl_init();
-    curl_setopt($ch, CURLOPT_URL, "localhost:8080?user=1");
-    curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
-    $data = curl_exec($ch);
-    curl_close($ch);
-	return json_decode($data, true);
+	return curl_request("localhost:8080?user=1");
+}
+
+function get_posts_comments(int $post_id): array {
+	return curl_request("localhost:8080?post_id=" . $post_id);
 }
 
 $server->on('request', function(Request $request, Response $response) {
@@ -47,6 +50,20 @@ $server->on('request', function(Request $request, Response $response) {
         go(function() use (&$users, $user, $key, $wg) {
             $wg->add();
             $posts = get_users_posts($user['email']); // ~1 sec
+
+            $wg2 = new WaitGroup();
+            
+            foreach ($posts as $post_id => $post) {
+            	go(function() use (&$posts, $post_id, $wg2) {
+		            $wg2->add();
+		            $comments = get_posts_comments($post_id); // ~1 sec
+		            $posts[$post_id]['comments'] = $comments;
+		            $wg2->done();
+		        });
+            }
+
+            $wg2->wait(5);
+
             $users[$key]['posts'] = $posts;
             $wg->done();
         });
